*Test case 1:*
1. first_movie= driver.find_elements(By.XPATH,"//div[2]/div/div/div[2]/h4[@class='card-title mb-1 name']/a")--> ko bk rút gọn hơn
+first_movie= driver.find_elements(By.XPATH,'//*[@id="app"]//h4[@class="card-title mb-1 name"]/a')
==> Chỗ này nếu dùng id thì tại sao ko dùng id='showtimes', nó gần hơn. Nhưng nếu rút gọn như anh thì nó đang lấy sai, nó lấy tên của phim sắp chiếu chứ không phải phim đầu tiên. khi ctrl f thì xpath đó là chung chứ ko phải duy nhất thèn đầu tiên.
+ Updated

2. time_container = movie.find_element(By.XPATH, "//*[@id='showtimes']/div/div[2]/div/div/div[2]/div/div") --> ko bk rút gọn dong nay
+ time_container = movie.find_element(By.XPATH, "//*[@id='showtimes']//div[@class='mb-1']")
==> cái ni tương tự là xpath chung luôn, nó ko duy nhất, nhưng nó vẫn chạy đúng, là sao? 
+ Updated

3. time_slots[0].click()  --> ko chọn đươc giwof, lỗi
+ Vì cái time_slots[0] là tag <span> chớ ko phải tag <a> nên ko click đc. Cái time_slots là đang lấy 1 list các tag <span>.
Cho nên phải từ cái time_slot[0] tìm cái parent của hắn là tag <a> thì mới click đc.
++ Vấn đề thứ 2: do trang web ni luôn hiển thị tất cả giờ chiếu trong ngày, kể cả giờ đã chiếu, nên những giờ đã qua ko thể click đc --> cái này sẽ gây lỗi.
VD: hiện tại là 23:52, thì khi click vô giờ đầu tiên trong ngày hôm nay thì sẽ ko click đc nữa, vì đã qua rồi.
+++ Nên giờ sửa test case lại chút: thay vì lấy giờ sớm nhất của ngày hiện tại thì lấy giờ sớm nhất của ngày kế tiếp (ngày mai), bằng cách click vô cái ô ngày giờ kế tiếp,
rồi mới lấy suất chiếu đầu tiên và click vô.

==> Cần lưu ý 2 v đề:
+ Khi màn hình nhỏ hơn 1000 px nó sẽ giấu mấy cái menu như "Rạp" đồ đi, ko thể click đc.
Nên bổ sung driver.set_window_size(1024, 800) để nó luôn cho cái màn hình luôn luôn thấy đc mấy cái menu để click vô.

+ Khi dùng XPATH thì nên dùng ngoặc đơn ở ngoài, ngoặc kép ở trong. Vì khi copy từ F12 về thì nó luôn dùng "" ở trong, mình đỡ mất công phải đổi thành '' cho từng cái, phí time.
VD: first_movie= driver.find_elements(By.XPATH,'//*[@id="app"]//h4[@class="card-title mb-1 name"]/a') sẽ tốt hơn là (By.XPATH,"//*[@id='app']//h4[@class='card-title mb-1 name']/a"
==============================
*Test case 2:*
1. không làm đc phần language
2. ko lấy list movie đúng--> ko click phim 2
3. phần phía dưới viết nhưng chưa chạy đúng hoàn toàn vì bị stuck bởi những lỗi sai ở trên, nên sai thì sửa lun
4. check phần rút gọn vì thấy rút khá phèn, mặc dù cũng ra đúng


*Note: 
1. tên biến luôn đặt in thường, ko viết in hoa (vd: list thay cho List). Sửa lại hết tất cả tên viết hoa (List_movie, Latest, Act, ...)
2. Đã sửa language
3. Đã fix click movie 2 - v đề là sau khi chọn thể loại này nọ thì nó chỉ hide những film ko phù hợp đi thôi, nhưng lấy list thì vẫn tồn tại.
Nên phải lọc những movies ko thấy đc ra thì mới chọn đúng cái thứ 2
4. Đoạn chọn rạp cgv phải nghiên cứu làm lại cho chuẩn vì cái phim nớ có thể ko chiếu ở rạp vincom DN nên ko có trong list hoặc ko chiếu ở tất cả các rạp CGV luôn.
Đây là practice: nếu ko có rạp mình chọn thì click rạp CGV đầu tiên có chiếu, nếu ko có rạp CGV nào chiếu thì chọn cụm rạp khác, lấy cái đầu tiên có chiếu cũng đc. 
Tham khảo cách viết if ... else ... trong code.

***5. chọn location dn để có thể check case khác, đn mới có vincome hoặc vinh trung, ko chọn sao check. 
và khi chọn thì rõ ràng vẫn có rạp CGV cho phim đó chứ ko phải ko có. nếu ko có thì đã viết khác rồi nhưng rõ ràng là có và ko click đc. 
xem và chỉnh lại đàng hoàng tc 2
==============================
*Test case 3:*
1. những cái # là sai, ngoài ra vẫn còn sai nhiều cái khác, check lại